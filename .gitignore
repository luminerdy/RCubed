# Runtime state (per-machine, per-boot)
config/robot_state.json

# Captured images and datasets (too large for git; keep a backup elsewhere)
data/
scans/
training_scans/
training_scans_backup_*/
*.jpg
*.jpeg
*.png
*.log

# Trained models: commit only deliberately, by name
models/*.pkl
models/*.onnx
models/*.pt

# Python
__pycache__/
*.pyc
*.pyo
.pytest_cache/
*.egg-info/
build/
dist/
.env
venv/
.venv/

# IDE / OS
.vscode/
.idea/
.DS_Store
tmp/
*.bak
