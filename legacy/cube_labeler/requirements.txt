# The labeler's dependencies are a subset of the project's.
# Install from the repo root instead:
#
#   pip3 install --break-system-packages -r ../requirements.txt
#
# Previously this file pinned opencv-python==4.8.0.76 and numpy==1.24.3, which
# would downgrade (and break) the versions the rest of the project runs on.
-r ../requirements.txt
